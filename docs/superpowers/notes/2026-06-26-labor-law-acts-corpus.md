# Расширение корпуса актами трудового права (2026-06-26)

Трек A роадмапа (`2026-06-23-sps-parity-roadmap.md` §3-A): добираем нишу
трудового права точечно. Добавлены 2 проверенных ФЗ; 2 — в реестр ожидаемых
(`PendingAct`), т.к. headless-источник не найден.

## Метод (тот же конвейер, что для 23 кодексов, см. 2026-06-25-rf-codes-corpus.md)

1. **Дискавери `nd=`:** `WebSearch` с `allowed_domains=["pravo.gov.ru"]`. ИПС-поиск
   по-прежнему недоступен headless (фреймсет→500), поэтому название→`nd=` напрямую
   не резолвится; выдача шумная (часто только поправки, а не основной акт).
2. **Гейт (обязательный):** живой `fetch(doc_itself&nd=X&print=1)` +
   `parse_document` (оба чистые от Django/БД). Принимался только если детектнутый
   заголовок/шапка совпали с ожидаемым актом И иерархия без «сирот». Непроверенный
   `nd=` НЕ сеем (риск порчи корпуса).

## Добавлено в SEED_ACTS (verified live fetch+parse 2026-06-26)

| slug | nd= | № | глав/разд | статей | сирот | дата ред. |
|------|-----|---|-----------|--------|-------|-----------|
| nesch-125-fz | 102054606 | 125-ФЗ | 8 глав | 74 | 0 | 2025-11-28 |
| sever-4520-1 | 102021716 | 4520-1 | 8 разд | 22 | 0 | 2023-12-25 |

- **125-ФЗ** «Об обязательном социальном страховании от несчастных случаев на
  производстве и профессиональных заболеваний» (24.07.1998). Заголовок детектнут
  точно.
- **4520-1** «О государственных гарантиях и компенсациях для лиц, работающих и
  проживающих в районах Крайнего Севера…» (19.02.1993, «Закон РФ» —
  дореформенная нумерация, не -ФЗ). `detect_title` спотыкается о шапку
  «ЗАКОН / РОССИЙСКОЙ ФЕДЕРАЦИИ» (даёт «РОССИЙСКОЙ ФЕДЕРАЦИИ») — заголовок
  поэтому задан в сиде вручную; содержание подтверждено по первым строкам текста.

**Решения по флагам:** `doc_type="federal_law"`, `level="federal"`,
`source_status="official"`, `auto_ingest=True` (приёмка парсера пройдена),
**`auto_publish=False`** — консервативно, как у прочих ФЗ трудового права
(426/10/82): демон даёт лишь ЧЕРНОВИКИ куратору, без авто-публикации (в отличие
от ТК РФ и кодексов, где founder выбрал авто-консолидацию).

`official_pub_date` не заполняем (нет надёжного источника; реквизит-идентичность
не выдумываем — карточка покажет «—»). `detected_number` парсера на этих актах
даёт мусор (181-ФЗ / 4-ФЗ — это номера поправок, первый match); официальный номер
задан вручную — штатное поведение (наивная подсказка, не источник истины).

## В PendingAct (источник не найден headless)

- **156-ФЗ** «Об объединениях работодателей» (27.11.2002). WebSearch отдаёт только
  поправки (355-ФЗ 2015 и т.п.), основной `nd=` не виден.
- **255-ФЗ** «Об обязательном социальном страховании на случай временной
  нетрудоспособности и в связи с материнством» (29.12.2006). Видны только поправки
  (455-ФЗ 2025, nd=102431338…) и отменённый предшественник (nd=102111246).
- **565-ФЗ** «О занятости» — уже было (только PDF-скан на publication, OCR не делаем).

Разблокировка: `nd=`-URL основного консолидированного текста из реального браузера
(или Claude-in-Chrome) → прогнать через гейт fetch+parse → перенести в SEED_ACTS.

## Хвост (user-gated)

После мержа: на dev-БД `manage.py seed_corpus` (заведёт 2 новых акта +
3 PendingAct, проставит флаги) → ежедневный `daily-sweep` или разовый
`manage.py ingest_url --slug nesch-125-fz --url <source_url>` → ЧЕРНОВИК куратору
на приёмку/публикацию.
